# Database — update host/credentials to match your setup
DATABASE_URL="postgresql://vendorbridge:vendorbridge_pass@localhost:5432/vendorbridge"

# JWT
JWT_SECRET="change_this_to_a_strong_random_secret"

# Server
PORT=3001

# SMTP (optional — for invoice email sending)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER=""
SMTP_PASS=""
